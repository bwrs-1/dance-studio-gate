import { Plugin } from '@nuxt/types';
import { Content, Form, GetParams } from '@spearly/sdk-js';
export declare type ListData = {
    contents: Content[];
};
export declare type ContentData = {
    content: {
        createdAt: Date | null;
        updatedAt: Date | null;
        publishedAt: Date | null;
    } & Omit<Content, 'createdAt' | 'updatedAt' | 'publishedAt'>;
};
export declare type FormData = {
    form: {
        createdAt: Date | null;
    } & Omit<Form, 'createdAt'>;
};
export declare type Props = {
    id: string;
};
export declare type ListProps = {
    order?: string;
    orderBy?: string;
} & Omit<GetParams, 'order' | 'orderBy' | 'orderDirection'> & Props;
declare const plugin: Plugin;
export default plugin;
