import type { AnalyticsPostParams } from '../types';
import { SpearlyAnalyticsApiClient } from './SpearlyAnalyticsApiClient';
export declare class SpearlyAnalytics {
    client: SpearlyAnalyticsApiClient;
    distinctId: string;
    sessionId: string;
    constructor(domain?: string);
    pageView(params: AnalyticsPostParams): Promise<void>;
    conversion(params: AnalyticsPostParams): Promise<void>;
    private setDistinctId;
    private setSessionId;
    private setCookie;
    private getCookie;
}
