import { Content, ServerContent } from './Content';
export declare type List = {
    totalContentsCount: number;
    matchingContentsCount: number;
    limit: number;
    offset: number;
    next: number;
    data: Content[];
};
export declare type ServerList = {
    data: ServerContent[];
} & Omit<List, 'data'>;
